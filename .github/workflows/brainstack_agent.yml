name: Brainstack Agent

on:
  push:
    branches:
      - main
    paths:
      - 'Packages/@brainstack/agent/**'

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set up Node.js
        uses: actions/setup-node@v2
        with:
          node-version: 14

      - name: Install dependencies
        working-directory: Packages/@brainstack/agent
        run: npm install

      - name: Build package
        working-directory: Packages/@brainstack/agent
        run: npm run build

      # - name: Run tests
      #   working-directory: Packages/@brainstack/agent
      #   run: npm test

      - name: Publish to npm
        if: github.ref == 'refs/heads/main'
        working-directory: Packages/@brainstack/agent
        run: npm publish
        env:
          NPM_TOKEN: ${{ secrets.NPM_TOKEN }}
